Add image files here to test the batch resizer.

Supported formats: .jpg, .jpeg, .png, .bmp, .gif, .tiff, .webp

Example:
  Copy some photos here, then run:
  python main.py sample_data/ --width 800

The resizer will create an output/ folder with the resized images.
